# 业务数据与存储建议

本文是方案建议，尚未执行数据库迁移或替换现有内存 Map。

## 当前会丢失的数据

`KnowledgeBaseController` 将知识库、文档文本和商品关联关系保存到内存；`ProductController` 将商品和每个商品的最新讲解保存到内存；`SpeechController` 将其自身接口创建的任务保存到内存。服务重启会丢失这些数据，商品会恢复为硬编码示例。讲解按商品 ID 覆盖，重新生成时旧版本即不可查询。商品音频接口当前返回的任务 ID 并未写入语音任务表或 Map。

## 推荐方案：MySQL 8 + OSS，Redis 按需使用

项目已经依赖 MySQL、MyBatis-Plus、OSS 和 Redis，沿用它们可以减少接入和维护成本。

| 数据 | 主要字段或内容 | 推荐存储 |
| --- | --- | --- |
| 商品 | 名称、规格、卖点、适用人群、禁用描述、上下架状态 | MySQL `product` |
| 知识库 | 名称、管理归属、访问范围、启用状态 | MySQL `knowledge_base` |
| 知识库文档 | 文件名、类型、对象 key、文件摘要、解析状态、错误摘要、解析文本 | MySQL `knowledge_document`；原文件放 OSS |
| 知识片段 | 文档 ID、知识库 ID、片段序号、正文、页码或段落来源 | MySQL `knowledge_chunk` |
| 商品关联 | 商品与一个或多个知识库的关联 | MySQL `product_knowledge_rel` |
| 讲解版本 | 独立 ID、商品 ID、版本号、场景、语气、目标时长、正文、模型和提示词版本、引用片段、状态与时间 | MySQL `product_explanation`，按版本追加 |
| 语音任务 | 讲解版本 ID、供应商、音色、语速、格式、幂等键、状态、重试次数、超时与错误摘要、音频对象 key | MySQL `speech_task`；音频文件放 OSS |
| 导入和生成任务 | 类型、资源 ID、排队/处理中/成功/失败状态、幂等键、重试次数、开始与结束时间、错误摘要 | MySQL 任务表，支持重启后恢复或重试 |
| 缓存与限流 | 热门商品结果、短期状态缓存、调用频次、带过期时间的并发锁 | Redis；可从 MySQL 重建 |

## 实施要点

1. 建立实体、Mapper 和 Service，使用 MySQL 替代 Controller 内的 Map。现有 SQL 缺少知识片段、历史版本约束和完整任务字段，应先补充版本化迁移脚本。
2. 每次生成新增讲解记录，用独立 `explanation_id` 关联音频。对 `(product_id, version)` 建唯一约束，支持查最新、列历史和切回历史版本，避免覆盖文本。
3. 商品检索先通过关联表限定知识库范围，再在范围内检索片段；管理端权限与业务归属需单独落实，数据库关联不能替代鉴权。
4. 原始 PDF、DOCX、XLSX 和 MP3 放 OSS。数据库保存稳定对象 key 和文件元数据；读取时按需生成有效期有限的访问 URL，避免把临时签名 URL 当永久文件地址。
5. MySQL 任务状态为准：请求先落库再处理，增加超时、重试和幂等约束。Redis 不作为业务数据唯一存储，锁也不能代替数据库唯一约束。
6. 第一阶段可在 MySQL 中做受知识库范围约束的关键词检索。中文资料需要中文分词或 ngram 索引，并验证召回；资料量和语义检索需求明确后再评估向量索引。
7. 配置 MySQL 自动备份并验证恢复；OSS 按文档与音频设置保留和清理规则。模型及 OSS 密钥放服务端环境变量或密钥管理服务，不进入前端和普通业务表。

不建议以 JSON 文件、前端本地缓存或 Redis 单独保存正式业务数据。当前阶段无需额外引入向量数据库；MySQL + OSS 即可承担可靠持久化。
