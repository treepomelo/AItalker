# Super 功能完善草案

状态：头脑风暴方案，尚未实施。当前已运行的页面和接口不因本文自动改变。

## 1. 产品结构

保留两个彼此独立的用户页面，用底部导航切换：

- **实时聊天**：通用文本对话，支持流式输出、新建会话、历史会话、停止生成、失败重试和复制回答。
- **商品讲解**：选择商品、查看资料状态、设置讲解场景/语气/目标时长、生成与查看讲解、查看历史版本、生成及播放语音。

商品页为默认首页。聊天历史与商品讲解上下文分开，不把商品资料默认注入普通聊天。用户侧不恢复文件上传、图片上传和旧语音演示页。

管理功能独立于用户页面：商品管理、知识库及文档导入、商品关联知识库、模型/语音配置状态与连接测试、任务状态和错误记录。

待确认：实时聊天需要登录，还是两个用户页面均无需登录。无论用户页采用哪种方式，管理接口必须有独立身份和权限检查，不能沿用当前全局关闭鉴权的做法。

## 2. 当前两个问题的原因

1. 实时聊天页不见：此前将“不要匿名聊天”理解为下线整个聊天功能，`pages.json` 只注册商品页。应恢复实时聊天页面，并明确其访问规则。
2. 生成讲解反馈不清：后端没有真正调用模型，也不检查模型是否配置，而是把第一份文档或“暂无知识库资料”拼成成功结果。前端随后自动调用仅返回 `PENDING` 的音频接口；没有任务轮询，播放按钮只有拿到音频 URL 才显示。

## 3. 模型与语音配置

- 服务端统一管理模型地址、Key、模型名、超时与输出长度；客户端只选择服务端允许的模型 ID，不提交任意 URL 或 Key。
- 第一版先接一种 OpenAI 兼容文本供应商，聊天和商品讲解复用调用层，使用各自的提示词和上下文。
- 第一版使用服务端环境变量或本地忽略文件配置凭据；管理页展示脱敏配置状态并提供连接测试，不把 Key 下发前端。
- “配置完整”与“连接测试成功”分开显示。存在 Key 不代表 Key 有效，模型列表也不代表模型可以调用。
- 文本模型和语音模型独立配置，语音不可用不能阻止文本生成。
- 用户页只获得必要的能力状态与可读提示；供应商响应、堆栈、内网地址和敏感日志留在受保护的管理侧，并做脱敏。

## 4. 点击后必须有确定反馈

后端为最终校验方，前端提前展示状态不能替代服务端校验。

| 条件 | 用户看到的反馈 | 后端行为 |
| --- | --- | --- |
| 未选择商品 | 请先选择商品 | 不发起生成 |
| 文本模型未配置 | 尚未配置大模型，请管理员完成配置后重试 | HTTP 503，`MODEL_NOT_CONFIGURED`，不生成成功记录 |
| 商品未关联可用资料 | 当前商品暂无可用知识资料 | HTTP 409，`KNOWLEDGE_NOT_READY`，不编造讲解 |
| 文档仍在解析 | 商品资料正在处理中，请稍后重试 | 返回资料处理状态 |
| 生成进行中 | 排队中/生成中，可取消 | 返回真实任务 ID，防止重复创建 |
| 模型鉴权失败 | 模型服务认证失败，请联系管理员 | 记录脱敏错误，不把供应商 401 当成用户登录过期 |
| 模型超时 | 生成超时，请重试 | HTTP 504 或任务失败状态；清除前端加载状态 |
| 语音未配置 | 语音服务尚未配置，文字讲解可正常查看 | 不创建虚假语音任务 |
| 语音失败 | 语音生成失败，可重试 | 保留讲解正文，记录失败原因 |

统一响应包含 `code`、`message`、`requestId` 和必要的 `data`。任务轮询成功只表示读取状态成功；任务自身的失败状态必须单独判断。错误不要只靠瞬间消失的 Toast，应在对应消息或结果区域保留，并提供重试入口。

## 5. 实时聊天流程

1. 页面展示模型状态，支持选择已开放模型；未配置时仍能进入页面并知道原因。
2. 输入非空文本后创建消息，并立即显示正在连接/生成状态。
3. 沿用后端 WebSocket 能力，规范 `started`、`delta`、`completed`、`error`、`cancelled` 事件，携带会话、消息和请求 ID。
4. 前端按消息 ID 接收流式片段，避免全局监听重复注册造成多次回写。
5. 停止生成要取消服务端模型请求；离开页面清理监听和连接。
6. 断线或超时结束加载状态，保留已生成内容并标注未完成，支持重试；重连先查消息状态，不自动重复计费请求。
7. MySQL 保存会话和消息，刷新后能找回历史。会话必须校验归属；若最终选择免登录，需要服务端分配访客会话并限制访问和频率。

## 6. 商品讲解与语音流程

1. 商品列表有加载、空数据、错误和重试状态，商品详情显示规格与资料是否就绪。
2. 检查模型和商品资料后创建真实讲解任务；第一版采用任务状态查询，不必一次引入额外队列组件。
3. 检索必须先限定商品关联且有权访问的知识库，再查找片段；保存引用来源。
4. 只根据商品及检索资料生成，不编造价格、功效或承诺。
5. 每次成功生成保存独立讲解版本，展示时间、参数和引用，支持历史查看与重新生成。
6. 文本生成结束后即显示正文。语音由用户单独点击触发，不与生成文本串成一次不可分割的操作。
7. 相同讲解版本和相同语音参数复用音频；提供合成中、播放、暂停、继续、停止、失败重试状态。
8. 切换商品或离开页面时停止音频。过期请求按商品 ID 和请求 ID 丢弃，避免 A 商品的结果覆盖 B 商品。

## 7. 业务数据与任务可靠性

使用现有 MySQL 8 + OSS；Redis 用于缓存和限流，不能代替持久化。

- MySQL：商品、知识库、文档元信息和解析文本、知识片段、关联关系、讲解历史、会话、消息、任务及错误摘要。
- OSS：原始资料和音频。数据库保存对象 key，按需生成访问 URL。
- 将 Controller 内 Map 替换为实体/Mapper/Service；现有七张表不足以覆盖会话、片段、任务治理，需要版本化迁移。
- 导入、生成、语音任务具有排队、处理中、成功、失败、取消状态，落库后执行，设置超时、有限重试和幂等键。
- 第一版可用数据库任务表和有界线程池；重启时识别失联任务并标记或恢复，不能永远停在处理中。
- 文档导入仅管理端可用，支持 PDF、DOCX、XLSX，失败可查原因、可重试。

## 8. 建议实施顺序

1. **先恢复可理解的界面**：恢复双页面导航，明确聊天访问规则；补充模型/语音/资料状态和错误提示；删除成功占位返回和虚假任务。
2. **打通真实文本链路**：服务端模型配置、连接测试、流式聊天、商品讲解生成；同时把会话、商品资料和讲解版本落入 MySQL。
3. **补齐资料管理闭环**：商品 CRUD、文档上传/解析/切片/检索、权限隔离和引用追溯。
4. **接入语音**：真实供应商、OSS 存储、任务查询、缓存及播放器生命周期。
5. **完整验证**：用未配置、错误 Key、网络超时、无资料、成功、取消、重复点击、切换商品和服务重启场景验收。

## 9. 最小验收清单

- 实时聊天和商品讲解均可从导航进入，状态互不串用。
- 没有配置模型时，发送或生成明确提示“尚未配置大模型”，无假成功。
- 配置有效模型后，聊天持续显示流式输出，商品生成真实讲解。
- 没有商品资料时能说明缺什么，不生成无来源的宣传内容。
- 超时、取消和失败都能结束加载，原输入与已生成内容不丢失。
- 刷新页面和后端重启后，已保存的会话、资料与讲解历史仍可读取。
- 语音失败不影响正文；同一讲解重复播放不重复合成。
- 只有管理员能修改模型配置、商品和知识库；普通用户不能读取凭据或越权访问资料。
