# Super 项目改造任务书

## 一、项目目标

将当前“微信小程序 AI 聊天应用”改造成面向商品讲解的 AI 应用：

1. 前端保留商品讲解相关的文本交互，移除登录、文件上传和图片交互功能。
2. 后端保留文件解析能力作为知识库导入能力；图片理解功能暂不实现，不进入本期验收。
3. 增加知识库能力，根据知识库内容为每件商品生成商品讲解稿。
4. 前后端接入语音模型 API，将商品讲解稿转换为语音并播放。

## 二、范围与原则

### 保留内容

- 后端文件解析：PDF、DOCX、XLSX。
- 后端图片识别能力暂不纳入本期实现，保留现有代码和扩展位置供后续使用。
- OpenAI 兼容模型调用能力。
- OSS 文件存储能力，或替换为等价对象存储。
- 商品讲解文本生成能力。

### 移除或隐藏内容

- 前端微信登录、退出登录和登录态页面。
- 前端文件选择、文件预览、文件上传入口。
- 前端图片上传、图片识别入口和图片展示交互。
- 面向普通用户的 OSS 上传接口。

### 重要约束

- 模型地址、API Key、知识库管理接口必须由服务端控制，不能由前端传入任意 URL 或密钥。
- 知识库和商品数据必须隔离，生成讲解时只能检索授权知识库内容。
- 语音生成失败时，文本讲解仍然可用。
- 所有异步任务需要有状态、超时和错误记录。

## 三、任务拆分

## Task 1：前端改造

### 目标

将 Uni-app 前端改为无需登录的双页面应用：AI 实时聊天页面与商品讲解页面彼此独立，并移除文件和图片相关用户功能。

### 工作内容

1. 删除或下线登录流程：
   - 移除微信登录页、扫码登录页和退出登录按钮。
   - 删除前端 Token 初始化、刷新和登录拦截逻辑。
   - 路由启动后直接进入商品讲解主页。
2. 删除用户侧文件和图片功能：
   - 移除文件选择、上传、预览组件。
   - 移除图片上传、识图按钮和图片消息渲染分支。
   - 清理相关 API、状态管理和页面入口。
3. 拆分页面职责：
   - AI 实时聊天页面：保留通用文本聊天和流式回复能力，不承担商品讲解、文件上传或图片识别。
   - 商品讲解页面：只负责商品选择、讲解生成、讲解展示和语音播放。
   - 两个页面使用独立的页面状态、会话上下文和加载状态，不能共用同一段聊天历史。
4. 新增商品讲解页面：
   - 商品列表或商品选择器。
   - 商品基础信息展示。
   - 商品讲解文本展示，支持 Markdown 或纯文本。
   - “生成讲解”“重新生成”“播放语音”“停止播放”按钮。
   - 生成中、语音合成中、失败和重试状态。
4. 更新请求层：
   - 将请求改为匿名或服务端分配的短期会话 ID。
   - 增加商品列表、商品讲解、语音生成和音频地址接口。
   - 删除前端写死的开发机地址，改用环境配置。
5. 保留必要的聊天能力：
   - 如果商品讲解需要追问，保留文本对话能力。
   - 对话上下文必须绑定商品和知识库，避免跨商品串数据。

### 交付物

- 更新后的 Uni-app 页面、组件、API 和状态管理代码。
- 前端环境配置模板。
- 页面流程说明和接口调用说明。

### 验收标准

- 未登录时可以直接进入主页并查看商品。
- 前端界面和代码中不存在登录入口、文件上传入口和图片上传入口。
- 用户可以分别进入 AI 聊天页面和商品讲解页面，两个页面互不干扰。
- 用户可以在商品讲解页面选择商品、生成讲解、查看文本、播放和停止语音。
- 生成失败时页面能显示明确错误，并允许重试。
- 开发环境和生产环境可以通过配置切换后端地址。

## Task 2：后端知识库与商品讲解

### 目标

在后端建立知识库、商品和文档管理能力，根据知识库内容为每件商品生成可靠的讲解稿。

### 工作内容

1. 设计数据模型：
   - `knowledge_base`：知识库基本信息、状态、创建时间。
   - `knowledge_document`：文件地址、文件类型、解析状态、解析文本、错误信息。
   - `knowledge_chunk`：切分后的文本片段、来源文档、排序和检索字段。
   - `product`：商品名称、卖点、规格、适用人群、禁用描述等。
   - `product_knowledge_rel`：商品与知识库的关联关系。
   - `product_explanation`：生成版本、提示词版本、文本内容、状态和时间。
2. 建立知识库导入流程：
   - 管理端上传 PDF、DOCX、XLSX 文件资料。
   - 复用现有文件解析插件提取文本。
   - 图片理解解析暂不实现，相关接口和插件只保留扩展位置，不进入本期验收。
   - 统一转为标准文本，记录来源和解析错误。
   - 按长度和语义切分为知识片段。
3. 增加检索能力：
   - 第一阶段可使用 MySQL 关键词检索或简单匹配。
   - 后续预留向量数据库和 Embedding 接口。
   - 检索结果必须返回来源文档和片段信息，便于追溯。
4. 增加商品讲解生成服务：
   - 输入商品 ID、讲解场景、目标时长和语气。
   - 检索商品关联知识库内容。
   - 通过服务端配置的模型生成讲解稿。
   - 要求模型只使用检索内容，不得编造价格、功效、规格和承诺。
   - 保存生成版本，支持重新生成和回滚查看。
5. 增加接口：
   - 商品列表和商品详情。
   - 知识库及文档导入接口，限管理端使用。
   - 文档解析状态查询接口。
   - 商品讲解生成、查询和重新生成接口。
6. 增加任务治理：
   - 文档解析和讲解生成采用异步任务。
   - 增加排队、处理中、成功、失败、重试状态。
   - 增加超时、幂等键和日志脱敏。

### 推荐接口草案

```text
GET  /api/products
GET  /api/products/{id}
POST /api/admin/knowledge-bases
POST /api/admin/knowledge-bases/{id}/documents
GET  /api/admin/documents/{id}
POST /api/products/{id}/explanations
GET  /api/products/{id}/explanations/latest
```

### 交付物

- 数据库迁移脚本和实体/Mapper/Service/Controller。
- 知识库导入、解析、切分和检索服务。
- 商品讲解生成服务和接口文档。
- 讲解提示词模板及示例数据。

### 验收标准

- 管理端可以导入支持的 PDF、DOCX、XLSX 文件资料。
- 图片资料暂不参与知识库解析和商品讲解生成。
- 文档解析状态可查询，失败原因可追踪并支持重试。
- 商品可以关联一个或多个知识库。
- 生成讲解时只使用该商品关联知识库的内容。
- 生成结果可保存、查询、重新生成，且保留历史版本。
- 知识库管理接口不会被匿名前端直接调用。

## Task 3：语音模型 API 接入

### 目标

将商品讲解文本转换为语音，并在独立的商品讲解页面提供播放控制。

### 工作内容

1. 后端增加统一语音服务抽象：
   - 定义 `SpeechProvider` 接口。
   - 实现目标语音模型 API 适配器。
   - API Key、模型名、音色、语速和格式由服务端配置。
   - 预留多供应商切换能力。
2. 增加语音生成接口：
   - 输入讲解版本 ID或商品 ID。
   - 支持音色、语速、语言和输出格式配置。
   - 返回任务 ID、状态和音频地址。
   - 音频文件保存到 OSS，并设置合理的过期策略。
3. 增加缓存和幂等：
   - 相同讲解版本和语音参数直接复用已有音频。
   - 语音任务失败支持重试。
   - 限制文本长度和单用户/单商品生成频率。
4. 前端接入播放：
   - 点击播放时请求或查询音频地址。
   - 支持播放、暂停、继续、停止和播放结束状态。
   - 语音生成期间显示进度或等待状态。
   - 音频失败时保留文本讲解并提供重试。
5. 增加兼容性处理：
   - 统一音频格式，优先选择小程序支持稳定的格式。
   - 处理后台切换、页面销毁和网络中断。
   - 防止重复点击创建多个相同任务。

### 推荐接口草案

```text
POST /api/products/{id}/explanations/{explanationId}/audio
GET  /api/audio-tasks/{taskId}
GET  /api/products/{id}/explanations/{explanationId}/audio
```

### 交付物

- 语音供应商配置和 `SpeechProvider` 抽象。
- 语音生成、查询、缓存和 OSS 存储实现。
- 前端音频播放组件和状态处理。
- 语音 API 配置模板和联调文档。

### 验收标准

- 用户可以从商品讲解页面生成并播放语音。
- 同一讲解重复播放不会重复生成相同音频。
- 语音 API 失败时，文本功能不受影响。
- 支持停止播放和离开页面后的资源清理。
- API Key 不出现在前端代码、请求参数或日志中。

## 四、依赖关系与实施顺序

```text
Task 2：知识库与讲解服务
          │
          ├── 为 Task 1 提供商品和讲解接口
          └── 为 Task 3 提供稳定的讲解版本

Task 3：语音服务与播放
          │
          └── 依赖 Task 2 的讲解文本

Task 1：前端改造可先做页面清理，接口联调依赖 Task 2 和 Task 3
```

推荐顺序：

1. 先完成 Task 2 的数据模型、知识库解析和商品讲解接口。
2. 并行进行 Task 1 的前端清理和商品讲解页面开发。
3. 完成 Task 3 的语音服务后进行全链路联调。
4. 最后进行安全、性能和部署验证。

## 五、统一非功能要求

- 所有 API 返回统一 JSON 结构，错误包含可读消息和请求 ID。
- 日志不得输出 Token、API Key、完整文件内容和完整用户输入。
- 模型调用、文件解析和语音生成均需配置超时。
- 生产环境强制使用 HTTPS/WSS。
- 对外接口增加访问频率限制和参数长度限制。
- 增加后端集成测试、知识库检索测试和前端关键流程测试。
- 所有模型、OSS、微信和语音服务配置通过环境变量或密钥管理服务注入。

## 六、最终交付结果

完成后，用户可以无需登录地打开小程序，在独立的 AI 聊天页面进行通用实时对话，也可以进入独立的商品讲解页面，选择一个商品，查看由商品关联知识库生成的讲解稿，并点击按钮收听语音讲解。文件解析能力仅作为后端知识库导入能力存在；图片理解功能暂不实现，留待后续版本扩展。



